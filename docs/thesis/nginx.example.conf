# 示例：Nginx 托管 Vue 静态资源 + /api 反代至网关
# 使用方式：复制到 /etc/nginx/sites-available/ 并软链到 sites-enabled，修改 server_name、证书路径、root

server {
    listen 80;
    listen [::]:80;
    server_name your-domain.com;   # 改成你的域名

    # 若仅 HTTP 演示，可保留；生产建议下面整段改为 443 并 certbot
    root /var/www/mybs/dist;
    index index.html;

    location /api/ {
        proxy_pass http://127.0.0.1:9000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}

# HTTPS 示例（证书路径自行替换，可用 Let's Encrypt）
# server {
#     listen 443 ssl http2;
#     server_name your-domain.com;
#     ssl_certificate     /etc/letsencrypt/live/your-domain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
#     root /var/www/mybs/dist;
#     index index.html;
#     location /api/ {
#         proxy_pass http://127.0.0.1:9000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Forwarded-Proto https;
#     }
#     location / { try_files $uri $uri/ /index.html; }
# }
