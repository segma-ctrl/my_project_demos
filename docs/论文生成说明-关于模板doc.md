# 关于「论文模板-传给ai生成的那一份.doc」的说明

## 1. 为何未在原 `.doc` 上直接改写

- 仓库工具**无法读取/写入**旧版 Word 二进制格式（`.doc`）。
- 已尝试通过本机 **Word COM** 从 `e:\bs\` 提取文字；因路径下存在**多个** `.doc` 文件且控制台编码限制，**无法 100% 确认**哪一份即为你指定文件；提取到的正文壳为**中国矿业大学本科生毕业设计通用结构**（封面、声明、任务书、摘要目录、参考文献等占位样式），与「浮选」示例章节为同一类学校模板。

## 2. 交付物是什么

- 主文件：**`毕业设计论文-教学资源预约系统-本科毕设正文.md`**（与本说明同目录 `docs/`）。
- 内容为**完整论文正文**（绪论至结论、参考文献、致谢），对应你当前 **Vue3 + Spring Cloud Gateway + user/resource/booking 微服务 + MySQL + Redis/Redisson + Feign** 项目。
- **使用方法**：用 **Word** 打开该 `.md`（新版 Word 支持）或复制全文粘贴到你学校下发的 **`.doc` 模板**中对应章节位置，再应用模板的**标题样式、页眉页脚、页码、行距**等。

## 3. 请你本地务必再核对的两点

1. 将 `e:\bs\论文模板-传给ai生成的那一份.doc` **另存为 `.docx`** 后，若与当前提取结构不一致，以你 **`.docx` 中的章节目录为准**，把本 Markdown 各节标题对齐后粘贴。  
2. **参考文献**中带「请自查 CNKI」的条目，须在 **知网/万方** 用题名检索后**替换为正式卷期页码**，避免著录错误。

## 4. 可选清理

若不需要中间文件，可删除 `docs/` 下：`template_copy_*.doc`、`_extract_doc.ps1`、`_copy_templates.ps1`、`_template_extracted.txt`。
